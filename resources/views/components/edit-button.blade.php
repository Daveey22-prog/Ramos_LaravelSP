<a href="{{ $href }}" class="inline-flex items-center space-x-1 bg-yellow-600 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded-lg transition-colors duration-200">
    <i class="fas fa-edit"></i>
    <span>{{ $label ?? 'Edit' }}</span>
</a>
