<a href="{{ $href }}" class="inline-flex items-center space-x-1 bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg transition-colors duration-200">
    <i class="fas fa-eye"></i>
    <span>{{ $label ?? 'View' }}</span>
</a>
